# Overview of my notes

- `COMMANDS.md`: General-use commands I'll probably forget if I don't write them down.
- `MOTD.md`: How I set up the message of the day to print system uptime.

See also `make help` for custom commands in the top-level Makefile

