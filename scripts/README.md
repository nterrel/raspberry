# scripts for system administration

- `link-dotfiles.sh` symlinks a few files in `../dotfiles/` to `$HOME`.
- `tree.py` is a small python implementation of UNIX `tree`, ran with the `tree` command which is aliased globally. (WIP)
